services:
  - type: web
    name: smartbilling-whatsapp
    env: docker
    plan: starter # Recommended 'starter' plan (512MB RAM) because Chrome & Puppeteer are memory intensive
    envVars:
      - key: PORT
        value: 3000
      - key: PUPPETEER_SKIP_CHROMIUM_DOWNLOAD
        value: true
